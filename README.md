emergencyStop - Implementation needs to be added to set all lights to RED - 
API - http://localhost:8080/api/traffic-lights/emergency/stop


Ideally Change State Should Also Stop after Pause , But it is not working like this. 
Although we have set-state Function that will not work if system is paused. 
And will resume working if the system is resume. 
